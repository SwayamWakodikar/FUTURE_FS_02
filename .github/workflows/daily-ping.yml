
name: Keep Server Alive

on:
  schedule:
    - cron: "*/5 * * * *"   
  workflow_dispatch:        # Allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Send request to server
        run: |
          curl -fsS https://quantivecrm.onrender.com || exit 1
